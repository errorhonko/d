# 斗魂锻体计算器开发计划

## 产品假设

首版做成无需登录的响应式网页：用户选择英雄、初始属性、锻体轮次和每轮选项，页面实时计算最终属性，并能查看逐轮变化。具体公式和数据源在正式编码前确认，避免把未经验证的规则写死在界面里。

## 第一阶段：规则与数据建模

- 收集并核对当前版本的斗魂锻体规则、属性范围、随机/固定增长方式。
- 明确支持的属性：生命、攻击力、法强、攻速、护甲、魔抗、技能急速等。
- 用 TypeScript 定义英雄、基础属性、锻体选项、计算步骤和版本元数据。
- 将规则数据与计算函数分离，为版本更新和测试留出接口。

验收标准：给定一组手算样例，能得到明确且可复核的输入、步骤和预期结果。

## 第二阶段：计算核心

- 实现纯函数计算引擎，不依赖 React 组件。
- 处理百分比/固定值、叠加顺序、取整规则和上限/下限。
- 加入 Vitest 单元测试，覆盖正常值、边界值和版本回归样例。
- 为无法确定或存在随机性的结果展示区间或期望值。

验收标准：核心公式测试通过，计算结果可输出逐步明细。

## 第三阶段：最小可用界面

- 英雄搜索与选择、等级/基础属性配置。
- 锻体轮次与选项录入，可添加、删除、重排和重置。
- 实时展示最终属性、增量、逐轮计算过程和错误提示。
- 完成桌面端与手机端的基础响应式布局。

验收标准：用户可在一个页面完成一次完整计算，刷新前操作稳定无报错。

## 第四阶段：体验与数据维护

- 使用 URL 参数或 localStorage 保存、分享计算方案。
- 标注游戏版本、数据更新时间和公式说明。
- 增加常用方案、英雄头像和属性筛选。
- 如数据需要在线更新，增加服务端代理或构建期同步脚本；密钥不得放入前端环境变量。

验收标准：方案可恢复或分享，版本来源清晰，数据更新流程有文档。

## 第五阶段：发布

- 执行 lint、类型检查、单元测试和生产构建。
- 检查移动端、主流浏览器、空状态与无效输入。
- 部署到静态托管平台，并设置基础错误监控与更新记录。

验收标准：生产地址可访问，关键计算样例与本地一致。

## 建议的下一步

先整理一份“当前版本锻体公式 + 3～5 个游戏内实测样例”。拿到这份规则基线后，优先完成第一、二阶段，再开始做视觉界面。
